#include <stdio.h>
int main()
{
    /**
     * @brief
     * 先将被除数和除数都按照正数来进行运算，
     * 然后观察被除数(左操作数)的正负，若被除数为正，则运算结果为正，若被除数为负，则运算结果为负
     * 运算结果的正负与右操作符无关
     *
     */
    int x = -13 % 3;
    printf("%d", x);
}